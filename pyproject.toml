[project]
name = "horsey-scraper"
version = "1.0.0"
description = "Horse-racing lay-price scraper + each-way arb finder (Betfair + PaddyPower)"
requires-python = ">=3.11"
dependencies = ["playwright>=1.42", "certifi>=2024.2.2"]

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
betfair-scraper = "betfair_scraper.cli:main"
paddypower-scraper = "paddypower_scraper.cli:main"
arb-finder = "arb_finder.cli:main"
sport888-scraper = "sport888_scraper.cli:main"
novibet-scraper = "novibet_scraper.cli:main"

[tool.hatch.build.targets.wheel]
packages = [
    "src/common",
    "src/betfair_scraper",
    "src/paddypower_scraper",
    "src/arb_finder",
    "src/sport888_scraper",
    "src/novibet_scraper",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
    "integration: live network/browser test, opt-in via RUN_INTEGRATION=1",
]
